# 2026-03-22

## 安全修复完成

### 问题
两份代码分析报告指出敏感信息泄露风险：
- README.md 公开暴露生产 URL 和微信小程序 AppID
- TOOLS.md 包含服务器 IP
- 数据库密码硬编码

### 修复内容
1. **移除敏感信息**
   - README.md → 移除生产 URL 和 AppID，改为引用 `.env.example`
   - TOOLS.md → 移除服务器 IP 和生产域名
   - USER.md → 移除敏感配置

2. **环境变量配置**
   - 创建 `projects/scientific-release/backend/.env.example` 模板
   - 生产服务器已有 `.env` 文件（包含 WECHAT_SECRET 和 DB_PASSWORD）
   - 更新 systemd 服务配置使用 `EnvironmentFile` 加载环境变量

3. **服务重启**
   - `systemctl daemon-reload && systemctl restart scientific-release`
   - 健康检查通过：`{"status":"ok"}`

### Git 提交
```
Commit: 1977621
Message: 🔒 安全修复：移除敏感信息 + 环境变量配置
```

### 待办（后续）
- 添加 express-validator 输入验证
- 添加 express-rate-limit 请求限流
- 为核心 API 添加单元测试

---

## 内存情况
- 总量：1.8GB
- 已用：1.4GB (78%)
- 可用：386MB
- 无 swap 配置

## 备份清理
- 删除旧备份 `backup-20260318-052600.tar.gz`
- 保留最新备份 `backup-manual-20260321-124225.tar.gz` (145MB)
